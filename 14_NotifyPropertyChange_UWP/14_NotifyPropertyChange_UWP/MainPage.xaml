<Page
    x:Class="_14_NotifyPropertyChange_UWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:_14_NotifyPropertyChange_UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Models="using:_14_NotifyPropertyChange_UWP.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.DataContext>
        <Models:ClsPersona></Models:ClsPersona>
    </Page.DataContext>
    <RelativePanel>
        <TextBox x:Name="TextBox01" Text="{Binding nombre, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"></TextBox>
        <TextBox x:Name="TextBox02" Text="{Binding nombre, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" RelativePanel.Below="TextBox01"></TextBox>
    </RelativePanel>
</Page>
